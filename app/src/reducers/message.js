const initialState = {
    displayText: "test",
};

const message = (state = initialState, action) => {
    switch (action.type) {
        default:
            return state;
    }
};

export default message;
